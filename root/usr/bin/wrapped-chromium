#!/bin/bash

BIN=/opt/chromium/chrome

# Cleanup
if ! pgrep chromium > /dev/null;then
  rm -f $HOME/.config/chromium/Singleton*
fi

${BIN} --no-first-run --password-store=basic --start-maximized ${CHROME_ARGS} "$@" > /dev/null 2>&1
